# Display lazygit
bind -r g display-popup -d '#{pane_current_path}' -w80% -h80% -E lazygit

# Run Claude Code in a separate session
bind -r y run-shell '\
  SESSION="claude-$(echo #{pane_current_path} | md5sum | cut -c1-8)"; \
  tmux has-session -t "$SESSION" 2>/dev/null || \
  tmux new-session -d -s "$SESSION" -c "#{pane_current_path}" "claude"; \
  tmux display-popup -w80% -h80% -E "tmux attach-session -t $SESSION"'

# Live tail of the local-implementor (opencode) run for the current repo.
# Absolute-ish path: popup shells are non-interactive (no .zshrc), so
# ~/.scripts may not be on PATH there.
bind -r o display-popup -d '#{pane_current_path}' -w80% -h70% -E "~/.scripts/cc-impl-status -f"
