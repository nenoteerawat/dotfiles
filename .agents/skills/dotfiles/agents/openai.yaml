interface:
  display_name: "Dotfiles"
  short_description: "Repo-specific patterns and workflows for dotfiles"
  default_prompt: "Use the dotfiles repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true